# Отчет об архитектурных и логических ошибках в проекте Regan

## 🔴 Критические проблемы

### 1. **Утечка памяти: DOM event listeners не очищаются при unmount**

**Файл:** `packages/regan/src/utils/listeners.ts`

**Проблема:** `LisneterManager` добавляет event listeners к DOM элементам, но при удалении элементов (unmount) эти listeners не удаляются автоматически. Это приводит к утечкам памяти.

**Местоположение:**
- `LisneterManager.add()` добавляет listeners
- `LisneterManager.remove()` удаляет listeners, но вызывается только при изменении props
- При полном удалении элемента (например, в `detachChildren`) listeners остаются висеть

**Решение:** Нужно добавить метод `cleanup()` в `LisneterManager` и вызывать его при unmount элементов.

---

### 2. **Утечка памяти: listeners не очищаются при замене элементов**

**Файл:** `packages/regan/src/v/handle.ts:107-131`

**Проблема:** В функции `patchElement` при замене элемента через `replaceFull()` старые listeners из `vOld.listenerManager` не очищаются. Новый элемент создается с новым `listenerManager`, но старые listeners остаются на старом элементе.

**Решение:** Перед заменой элемента нужно очистить все listeners из `vOld.listenerManager`.

---

### 3. **Race condition в UpdaterTaskAsync**

**Файл:** `packages/regan/src/updater/updater.ts:19-42`

**Проблема:** 
- Переменная `timer` объявлена, но не используется в `setTimeout`
- Если `cancel()` вызывается после `add()` но до выполнения `setTimeout`, `timer` не очищается
- Множественные вызовы `add()` могут создать несколько таймеров

**Код:**
```typescript
class UpdaterTaskAsync {
  timer?: ReturnType<typeof setTimeout>;
  
  add(func: AnyFunc) {
    // ...
    setTimeout(() => { ... }); // timer не сохраняется!
  }
  
  cancel() {
    clearTimeout(this.timer); // timer всегда undefined!
  }
}
```

**Решение:** Сохранять возвращаемое значение `setTimeout` в `this.timer`.

---

### 4. **Потенциальная утечка памяти в AtomWrapper**

**Файл:** `packages/regan/src/components/atom-wrapper/atom-wrapper.tsx:48-93`

**Проблема:** Callback `cb` может быть вызван после того, как компонент размонтирован. Проверка `hNode.unmounted` отсутствует.

**Решение:** Добавить проверку `hNode.unmounted` перед выполнением обновлений.

---

## 🟡 Серьезные проблемы

### 5. **Дублирование кода: двойной вызов cancel()**

**Файл:** `packages/regan/src/render/render.ts:61,70`

**Проблема:** `areaCtx.updaterInit.cancel()` вызывается дважды:
- В блоке `try` после успешного рендеринга (строка 61)
- В блоке `finally` (строка 70)

**Решение:** Убрать вызов из блока `try`, оставить только в `finally`.

---

### 6. **Избыточное присваивание clientCtx**

**Файл:** `packages/regan/src/hydrate/hydrate.ts:27-35`

**Проблема:** `clientCtx` присваивается дважды:
- В конструкторе `GlobalCtx` (строка 31)
- После создания (строка 35)

**Решение:** Убрать избыточное присваивание на строке 35.

---

### 7. **Отсутствие проверки на null/undefined для clientCtx**

**Файл:** `packages/regan/src/components/atom-wrapper/atom-wrapper.tsx:67,78`

**Проблема:** Используется `hNode.globalCtx.clientCtx.window` без проверки на `undefined`. В серверном режиме `clientCtx` может быть `undefined`.

**Решение:** Добавить проверку или использовать non-null assertion только в клиентском режиме.

---

### 8. **Потенциальная проблема с циклическими ссылками**

**Файл:** `packages/regan/src/ctx/ctx.ts:80`

**Проблема:** `this.ctx = this` создает циклическую ссылку. Хотя это может быть намеренно, это может вызвать проблемы при сериализации или отладке.

**Решение:** Рассмотреть альтернативный подход или добавить комментарий с объяснением.

---

## 🟢 Орфографические ошибки

### 9. **Опечатка: ErrorGurard → ErrorGuard**

**Файлы:** Множественные файлы

**Местоположения:**
- `packages/regan/src/components/error-guard.tsx:9`
- `packages/regan/src/regan.ts:14`
- И другие файлы, использующие этот компонент

**Решение:** Переименовать `ErrorGurard` в `ErrorGuard` во всех файлах.

---

### 10. **Опечатка: chidlren → children**

**Файлы:**
- `packages/regan/src/render/component.ts:59,74`
- `packages/regan/src/hydrate/component.ts:52,63`
- `packages/regan/src/stringify/component.ts:44,52`
- `packages/regan/src/components/error-guard.tsx:11,21,24`

**Решение:** Исправить опечатки во всех местах.

---

### 11. **Опечатка: subsriber/subsribe → subscriber/subscribe**

**Файлы:**
- `packages/regan/src/updater/updater.ts:6,54,59,62,80,90`
- `packages/regan/src/atoms-tracker/atoms-tracker.ts:6,18,23,32`
- `packages/regan/src/utils/atom.ts:8,43`
- `packages/regan/src/utils/props.ts:66,70,84`
- `packages/regan/src/components/atom-wrapper/atom-wrapper.tsx:14,95`

**Решение:** Исправить опечатки во всех местах.

---

### 12. **Опечатка: LisneterManager → ListenerManager**

**Файл:** `packages/regan/src/utils/listeners.ts:10`

**Решение:** Переименовать класс и все его использования.

---

### 13. **Опечатка: Unknow → Unknown**

**Файлы:**
- `packages/regan/src/errors/errors.tsx:30`
- `packages/regan/src/stringify/stringify.ts:34`

**Решение:** Исправить опечатки.

---

### 14. **Опечатка: crate → create**

**Файл:** `packages/regan/src/v/handle.ts:159`

**Решение:** Исправить комментарий.

---

### 15. **Опечатка: rawHtm → rawHtml**

**Файл:** `packages/regan/src/v/handle.ts:200`

**Решение:** Исправить комментарий.

---

## 🔵 Проблемы архитектуры

### 16. **Отсутствие проверки unmounted перед обновлением**

**Файл:** `packages/regan/src/components/atom-wrapper/atom-wrapper.tsx:48`

**Проблема:** Callback `cb` не проверяет, не размонтирован ли уже `hNode` перед выполнением обновлений.

**Решение:** Добавить проверку `if (hNode.unmounted) return;` в начале callback.

---

### 17. **Несогласованность в обработке ошибок**

**Файл:** `packages/regan/src/render/render.ts:64-68`

**Проблема:** Обработка ошибок различается в зависимости от наличия `parentHNode`. Это может привести к несогласованному поведению.

**Решение:** Унифицировать обработку ошибок.

---

### 18. **Отсутствие cleanup для listenerManager при замене элементов**

**Файл:** `packages/regan/src/v/handle.ts:172`

**Проблема:** При полной замене элемента (`replaceFull`) старые listeners не очищаются из `vOld.listenerManager`.

**Решение:** Добавить очистку listeners перед заменой элемента.

---

### 19. **Потенциальная проблема с множественными подписками**

**Файл:** `packages/regan/src/utils/atom.ts:57-74`

**Проблема:** Если компонент монтируется несколько раз, может быть создано несколько подписок на один и тот же atom без соответствующей очистки.

**Решение:** Убедиться, что при повторном монтировании старые подписки очищаются.

---

## 📊 Резюме

**Всего найдено проблем:** 19
- 🔴 Критические: 4
- 🟡 Серьезные: 4
- 🟢 Орфографические: 7
- 🔵 Архитектурные: 4

**Приоритет исправления:**
1. Утечки памяти (проблемы 1, 2, 4)
2. Race condition (проблема 3)
3. Дублирование кода (проблема 5)
4. Орфографические ошибки (проблемы 9-15)
